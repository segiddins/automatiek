require 'spec_helper'

describe Automatiek do
  it 'has a version number' do
    expect(Automatiek::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
